def seek_information(query: str) -> None:
    """
    Description:
        Use this tool to seek for information as needed.

    Examples:
    >>> # Usage Example:
    seek_information("We need the departure date of the flight.")

    >>> # Usage Example:
    seek_information("We need the departure date of the flight and check in and check out dates for the hotel")
    """
    pass
